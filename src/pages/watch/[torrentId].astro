---
import WatchScreen from '@/features/torrents/screens/WatchScreen';
import HomeLayout from '@/layouts/HomeLayout.astro';

const { torrentId } = Astro.params;
if (!torrentId) return Astro.redirect('/');
---

<HomeLayout title="StreamTorrent — İzle">
  <WatchScreen client:load torrentId={torrentId} />
</HomeLayout>
